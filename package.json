{
  "name": "top-secret",
  "version": "0.0.1",
  "description": "A conference call implementation using WebRTC, Socket.io and Node.js.",
  "main": "src/app.js",
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "ejs": "^3.1.10",
    "express": "^4.17.1",
    "firebase": "^8.7.0",
    "serve-favicon": "^2.5.0",
    "socket.io": "^2.4.0"
  },
  "devDependencies": {
    "@types/socket.io": "^3.0.2",
    "nodemon": "^2.0.6"
  },
  "scripts": {
    "start": "node src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "nodemon  node src/app.js"
  },
  "icon": "favicon.icon"
}
